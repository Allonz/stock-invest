# Server configuration
server.port=8080

# Encoding configuration
spring.mandatory-file-encoding=UTF-8
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true
logging.charset.console=UTF-8
logging.charset.file=UTF-8

# Application logging - 移除这些旧配置
# logging.level.com.stock.invest=DEBUG
# logging.level.org.springframework=INFO
# logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36}:%line - %msg%n
# logging.file.name=logs/stock-invest.log
# logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36}:%line - %msg%n

# 新增日志目录配置 (用于创建目录结构)
logging.file.path=logs
logging.startup.path=logs/run
logging.application.path=logs/stock-invest

# UTF-8 Encoding configuration
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

# Default profile
spring.profiles.default=tiger

# Logging configuration
logging.file.name=logs/stock-invest/application.log
logging.level.root=INFO
logging.level.com.stock.invest=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36}:%line - %msg%n 